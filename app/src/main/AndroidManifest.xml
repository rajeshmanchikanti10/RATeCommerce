<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ratecommerce">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RATeCommerce"
        tools:replace="android:theme">
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/> <!-- optional (needed if default theme has no action bar) -->
        <activity android:name=".SettingsActivity"></activity>
        <activity android:name=".AdminManageAcitivity" />
        <activity android:name=".ConfirmorderActivity" />
        <activity android:name=".cartActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity
            android:name=".HomeAcitivity"
            android:label="@string/title_activity_home_acitivity"
            android:theme="@style/Theme.RATeCommerce.NoActionBar" />
        <activity android:name=".AdminconfigureActivity" />
        <activity android:name=".AdminAddNewProductActivity" />
        <activity android:name=".Register" />
        <activity android:name=".Login_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>